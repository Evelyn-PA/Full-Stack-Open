{
  "name": "part3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd../part2/phonebook && npm run build && cp -r dist ../../part3",
    "deploy:full": "npm run build:ui && git add . &7 git commit -m uibuild &7 git push"
  },
  "author": "Evelyn",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
